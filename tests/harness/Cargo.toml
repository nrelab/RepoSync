[package]
name = "reposync-harness"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Test harness for RepoSync: fixture-based golden and determinism tests."

[lints]
workspace = true

[dependencies]
reposync-core = { path = "../../crates/core" }
reposync-diff = { path = "../../crates/diff" }
reposync-git = { path = "../../crates/git" }
reposync-transform = { path = "../../crates/transform" }
git2.workspace = true
tempfile.workspace = true
