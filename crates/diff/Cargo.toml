[package]
name = "reposync-diff"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Diff engine for RepoSync: path/content-level snapshot diffs."

[lints]
workspace = true

[dependencies]
reposync-core = { path = "../core" }

[dev-dependencies]
reposync-transform = { path = "../transform" }
